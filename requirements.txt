# Runtime dependencies (deployed to Lambda)
# Note: boto3, botocore, s3transfer, jmespath, urllib3 are already in Lambda runtime
# Only add dependencies NOT available in Lambda

# Google Calendar API — for Google Meet session generation
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-httplib2>=0.1.1
